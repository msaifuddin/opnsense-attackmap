node_modules/
.env
docker-compose.nas.yml
# Everything here is generated at runtime: the GeoLite2 databases, and the
# rollup file - which holds real, unredacted addresses from your network.
# Ignore the whole directory rather than known filenames.
data/
*.log
*.tar.gz
