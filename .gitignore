node_modules/
.env
docker-compose.nas.yml
# Everything generated at runtime: the GeoLite2 databases, and rollup.json -
# which holds real, unredacted addresses from your network. Ignore the contents
# by default rather than named extensions, but keep the directory itself and its
# signpost tracked.
data/*
!data/.gitkeep
!data/README.md
*.log
*.tar.gz
